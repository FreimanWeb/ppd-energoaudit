# Расчётное ядро
pandas>=2.0
numpy>=1.24
scipy>=1.10
openpyxl>=3.1
pydantic>=2.0
pyyaml>=6.0
python-docx>=1.1     # парсинг текстовых отчётов энергоаудита (.docx)
# Конвертация бинарных .doc → .docx — внешний LibreOffice (soffice), см. README.

# Дашборды (app/)
streamlit>=1.30
plotly>=5.18

# Тесты
pytest>=7.4
