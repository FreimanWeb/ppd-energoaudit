# ВНИМАНИЕ: Streamlit Community Cloud этот файл НЕ читает, пока в репозитории
# есть uv.lock — приоритет файлов зависимостей: uv.lock > Pipfile >
# environment.yml > requirements.txt > pyproject.toml. Окружение на Cloud
# собирается из uv.lock (`uv sync`), поэтому реальный источник правды —
# [project.dependencies] в pyproject.toml. После правки зависимостей нужно
# выполнить `uv lock` и закоммитить uv.lock, иначе изменения на Cloud не приедут.
# Этот файл оставлен для локальной установки через pip.

# Расчётное ядро
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
xlrd>=2.0          # legacy .xls телеметрия (ingest/excel_telemetry.py)
pydantic>=2.0
pyyaml>=6.0

# Дашборд (app/)
streamlit>=1.30
plotly>=5.18
altair>=5.0
# Assets кастомного календаря входят в основной пакет ppd_audit.
# Streamlit Cloud устанавливает проект, чтобы обнаружить manifest компонента.
.

# Тесты
pytest>=7.4
