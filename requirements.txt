# Расчётное ядро
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
xlrd>=2.0          # legacy .xls телеметрия (ingest/excel_telemetry.py)
pydantic>=2.0
pyyaml>=6.0

# Дашборд (app/)
streamlit>=1.30
plotly>=5.18
altair>=5.0
# ppd_telemetry_calendar (локальный компонент календаря) намеренно НЕ здесь:
# если сборка этого пакета не проходит на Cloud, она рвёт установку ВСЕГО
# requirements.txt разом. app/main.py уже умеет работать без него (try/except).
# Если понадобится вернуть календарь — сначала проверить, что он ставится
# отдельно и не блокирует остальные пакеты.

# Тесты
pytest>=7.4
